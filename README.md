# Pipeline Leak Detection with LSTM Autoencoder

A complete Python application that detects anomalies in natural gas pipeline sensor data using an LSTM Autoencoder.

## Features
- Unsupervised anomaly detection (no labelled leak data needed)
- Real-time reconstruction error scoring
- Dynamic threshold with adjustable multiplier
- Interactive Streamlit dashboard

## Project Structure